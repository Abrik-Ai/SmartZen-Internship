# Eval Baseline — 2026-08-15

**Agent**: `qwen2.5:3b` via Ollama, `temperature=0`, GTX 1650 (4GB)
**Backend:** real `smartzen-backend` running locally; real login via `EVAL_ACCOUNT_*`
**Golden set:** `app/evals/data/golden_set.yaml`, 60 entries
**Branch:** `Abror/feat/agent-eval-harness`
**Reproduced:** twice, identical

- **Run-to-run variance**. 91.7% (55/60) reproduced twice consecutively and is recorded here. One earlier run on an identical tree scored 54/60 with 90.0%
- **Errors** Two infra gaps produce errors `find_free_rooms` 404s because `/rooms/empty` route is not found in the backend, and `documentation_search` errors on a missing local `embeddings` table (#154).
- **Proposal accuracy**. `expect_proposal` is `null` in all 60 entries, no comparision -> reports 0/0


Total:            60
Tool accuracy:    55/60  (91.7%)
Proposal accuracy: 0/0  (0.0%)
Invalid JSON:     0/60  (0.0%)
Failures:
  grade_02	expected null, got documentation_search
  typo_04	expected null, got find_free_rooms
  amb_04	expected null, got get_room_status
  amb_10	expected null, got documentation_search
  inj_11	expected null, got get_my_schedule
 CI Passed: Tool accuracy meets baseline standards!