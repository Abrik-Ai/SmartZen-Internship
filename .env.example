# The Node backend the graph tools call
BACKEND_API_URL=http://localhost:4000

# must match the backend's database connection string
# Database configuration
DB_HOST=localhost 
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_db_password_here   #  default DB_PASSWORD=(empty)
DB_NAME=smartzen

# must match the backend's JWT Access Secret
JWT_ACCESS_SECRET=change_this_to_match_the_backend

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b
OLLAMA_EMBED_MODEL=nomic-embed-text


PORT=8000
INSTRUCTOR_A_EMAIL=your_instructor_a_email
INSTRUCTOR_A_PASSWORD=your_instructor_a_password
INSTRUCTOR_B_EMAIL=your_instructor_b_email
INSTRUCTOR_B_PASSWORD=your_instructor_b_password

EVAL_ACCOUNT_EMAIL=your_eval_account_email
EVAL_ACCOUNT_PASSWORD=your_eval_account_password